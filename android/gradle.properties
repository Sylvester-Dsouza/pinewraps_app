org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=4G -XX:+HeapDumpOnOutOfMemoryError -Dkotlin.daemon.jvm.options=-Xmx4G
android.useAndroidX=true
android.enableJetifier=true
android.defaults.buildfeatures.buildconfig=true
android.nonTransitiveRClass=true
android.nonFinalResIds=false
org.gradle.parallel=true

# Java and Kotlin Versions
android.java.source.version=17
android.java.target.version=17
kotlin.jvm.target.validation.mode=warning

# Kotlin daemon settings
kotlin.daemon.enabled=true
kotlin.incremental=true

# Disable R8 full mode to avoid compatibility issues
android.enableR8.fullMode=false

# Suppress unsupported compileSdk warning

# Allow Kotlin metadata version mismatches
kotlin.metadata.jvm.suppressMetadataIncompatibilityWarning=true
kotlin.incremental.useClasspathSnapshot=false
kotlin.incremental=false
android.useAndroidX=true
android.enableJetifier=true

# Completely disable Kotlin metadata validation
kotlin.stdlib.default.dependency=false
kotlin.compiler.execution.strategy=in-process
android.suppressUnsupportedCompileSdk=35

# Suppress warnings
android.suppressUnsupportedOptionWarnings=android.suppressUnsupportedOptionWarnings,android.suppressUnsupportedCompileSdk
